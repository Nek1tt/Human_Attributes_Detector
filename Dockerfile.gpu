FROM nvidia/cuda:12.8.1-cudnn-runtime-ubuntu24.04

ENV DEBIAN_FRONTEND=noninteractive \
    PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    HAD_DATA_DIR=/app/var \
    HAD_DEVICE=cuda

RUN apt-get update \
    && apt-get install -y --no-install-recommends python3 python3-pip python3-venv ffmpeg libglib2.0-0 libgl1 \
    && rm -rf /var/lib/apt/lists/* \
    && groupadd --system app \
    && useradd --system --gid app --create-home app

WORKDIR /app
COPY pyproject.toml README.md ./
COPY src ./src
COPY SFSORT/SFSORT.py SFSORT/__init__.py SFSORT/LICENSE ./SFSORT/
RUN python3 -m pip install --no-cache-dir --break-system-packages ".[api,gpu]" \
    && python3 -m pip install --no-cache-dir --break-system-packages torch torchvision \
       --index-url https://download.pytorch.org/whl/cu128

RUN mkdir -p /app/models /app/var/uploads /app/var/outputs \
    && chown -R app:app /app
USER app

EXPOSE 8000
CMD ["python3", "-m", "uvicorn", "silhouette_detector.app:create_app", "--factory", "--host", "0.0.0.0", "--port", "8000"]
